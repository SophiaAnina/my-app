import { View, Text } from "react-native";

export default function FindScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>I am the Find Screen</Text>
      </View>
    );
  }